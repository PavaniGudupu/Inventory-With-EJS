section never uses name="" property
input type=hidden. it wont show.
❌ A button cannot have href=.

<form class="d-flex" action="/products" method="post" style="gap: 8px;">

this form will handle by this "/products" routes.
form will send using post method (req.body)
        "" ""      "" get method (req.query)


creating a object:
        const result = {
                current: {page, limit}
        }
        const result.page = {
                page: page + 1
        }


 categories: filterColumns.rows,




        const filterColumns = await db.query(`
      SELECT column_name
      FROM information_schema.columns
      WHERE table_name = 'products'
      ORDER BY ordinal_position;
      `);
    
      console.log(filterColumns.rows);


          <!-- <select name="filterCategory" class="form-select" style="width: 150px;">
      <option value="">All Categories</option>
    <% categories.forEach(cat => { %>
     <option value="P.<%= cat.column_name %>">
            <%= cat.column_name %>
     </option>
    <% }) %>

    </select> -->